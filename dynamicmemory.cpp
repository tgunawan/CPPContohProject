#include <iostream>
using namespace std;

// contoh penggunaan dynamic memory dengan class
/*
void stack(){
    int x=10;
    cout<<"Nilai x di stack: "<<x<<endl;
}

void heap(){
    int* h=new int(20); // alokasi memori di heap
    cout<<"Nilai x di heap: "<<*h<<endl;
    // delete h; // membebaskan memori yang dialokasikan wajib
}
int main(){
    // stack();
    for (int i=0;i<5;i++){
       heap();
    }   //contoh memory leak jika tidak ada delete h; karena setiap kali heap() dipanggil, memori baru dialokasikan tanpa dibebaskan
    
    return 0;
}*/ 

